/**
 * Schema: <%= name %> (<%= handle %>)
 * -----------------------------------------------------------------------------
 * Section settings for <%= nameLowerCase %> component.
 *
 */
module.exports = {
  name: '<%= nameTitleCase %>',
  settings: [],
  presets: [
    {
      name: '<%= nameTitleCase %>',
    },
  ],
  disabled_on: {
    groups: ['*'],
  },
}
