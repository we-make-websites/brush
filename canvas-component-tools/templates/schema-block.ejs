/**
 * Schema: <%= name %> (<%= handle %>)
 * -----------------------------------------------------------------------------
 * <%= liquidTitleCase %> settings for <%= nameLowerCase %> component.
 *
 */
module.exports = {
  name: '<%= nameTitleCase %>',
  settings: [],
  presets: [
    {
      name: '<%= nameTitleCase %>',
    },
  ],
}
